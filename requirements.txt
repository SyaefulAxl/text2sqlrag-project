# API Framework
fastapi[standard]  # Includes uvicorn[standard] with uvloop
pydantic
pydantic-settings
python-multipart
mangum  # AWS Lambda adapter for FastAPI

# Document Processing
docling                # Advanced PDF parsing with layout analysis
docling-core           # Chunking infrastructure
semchunk               # Semantic text splitting (used by HybridChunker)
unstructured[pdf]      # PDF support via poppler (NO PyTorch/ONNX - ARM64 compatible)
python-magic  # For file type detection (uses system libmagic on macOS)
pillow  # Image processing for document parsing
# Resume Processing
pdfplumber             # PDF text extraction
python-docx            # DOCX parsing
pdf2image              # PDF to image conversion for OCR

# Embeddings & Vector DB
openai
pinecone[grpc]         # Legacy - keeping for backward compatibility
qdrant-client          # NEW: Qdrant vector database
tiktoken

# Text-to-SQL
vanna[fastapi,openai,postgres]
sqlalchemy
psycopg2-binary
faker

# Evaluation & Monitoring
ragas
datasets
opik

# Utilities
python-dotenv
httpx

# Caching
numpy  # Efficient binary storage of embeddings
upstash-redis  # Legacy - keeping for backward compatibility
redis           # NEW: DragonflyDB client (Redis-compatible)
scikit-learn    # NEW: Advanced feature engineering (clustering, PCA)

# AWS Services (for S3 storage backend)
boto3  # AWS SDK for S3 storage

# Development
pytest
pytest-asyncio
black
ruff
moto[s3]  # Mock AWS S3 for testing
nest-asyncio>=1.5.8
